{
  "name": "pet-portre-orders",
  "version": "1.0.0",
  "private": true,
  "description": "Pet-Portre Orders API (Vercel) – Wix → Mongo → DHL (Create/Track) → Google Sheets",
  "license": "UNLICENSED",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"Vercel builds serverless functions; no build step required\"",
    "start": "echo \"Use vercel dev/start; no standalone server\""
  },
  "dependencies": {
    "mongodb": "^6.6.0"
  }
}
